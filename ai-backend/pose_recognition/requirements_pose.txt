# Pose Recognition 訓練流水線依賴
# 用法：在 venv 啟動後 pip install -r requirements_pose.txt

# --- 影像處理 + 關節點偵測 ---
mediapipe==0.10.14
opencv-contrib-python==4.10.0.84
numpy<2.0

# --- 訓練 ---
torch>=2.0,<2.5
pandas>=2.0
scikit-learn>=1.3

# --- ONNX 匯出 ---
onnx>=1.15
onnxruntime>=1.16

# --- 進度條（train_mlp.py 沒用到但常用）---
tqdm
